<!DOCTYPE html>
<html>
<head>
#parse("head.vm")
<title>直播来了 - 一个专注于游戏直播推荐的网站</title>
</head>
<body>
    #parse("nav.vm")

    <div id="banner" class="container"></div>

    <div class="container">
        <div class="row col-lg-offset-1">
            <ul class="list-inline">
            #foreach ($game in $games)
                <li #if ($currentGame == $game.abbr)class="text-primary bold" #end>
                    <a href="/s/$game.abbr/$currentPlatform/0">$game.name</a>
                </li>
            #end
            </ul>
        </div>
        <div class="row col-lg-offset-1">
            <ul class="list-inline">
            #foreach ($platform in $platforms)
                <li #if ($currentPlatform == $platform.abbr)class="text-primary bold" #end>
                    <a href="/s/$currentGame/$platform.abbr/0">$platform.name</a>
                </li>
            #end
            </ul>
        </div>
    </div>
    
    <div class="container" style="margin-top: 20px;">
        <div class="row">
        #foreach ($liveRoom in $liveRooms)
            <div class="col-sm-6 col-md-4 col-lg-3">
                <a class="card" target="_blank" href="/live/$liveRoom.platform.abbr/$liveRoom.game.abbr/$liveRoom.uid">
                    <div class="thumbnail">
                        <img class="card-img" src="$liveRoom.thumbnail" alt="$liveRoom.title">
                        <div class="caption">
                            <p class="card-title" title="$liveRoom.title">$liveRoom.title</p>
                            <span class="text-muted">
                                <span>&nbsp;</span>
                                <span class="pull-left">$liveRoom.views</span>
                                <span class="card-title col-sm-7 col-md-7 col-lg-7">$liveRoom.name</span>
                            </span>
                            <span class="pull-right">
                                <img class="zb-ico" src="$liveRoom.platform.icon" title="$liveRoom.platform.name" />&nbsp;
                                <img class="zb-ico" src="$liveRoom.game.icon" title="$liveRoom.game.name" />
                            </span>
                        </div>
                    </div>
                </a>
            </div>
        #end
        </div>
        #if (!$q)
        <nav>
            <ul class="pager">
                #set($p = $offset - 20)
                #set($n = $offset + 20)
                <li #if ($offset <= 0)class="disabled" #end><a href="/s/$currentGame/$currentPlatform/$p">前一页</a></li>
                <li><a href="/s/$currentGame/$currentPlatform/$n">后一页</a></li>
            </ul>
        </nav>
        #end
    </div>

    #parse("footer.vm")
</body>
</html>
